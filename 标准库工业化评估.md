# Synth-OOP 标准库工业化评估（重写版）

| 项 | 值 |
|---|---|
| 评估对象 | Syclun 标准库 `lib/*.synl` + `lib/cpp/*.hpp`（18 个模块，约 300 个公开方法与构造器） |
| 评估日期 | 2026-09-25 |
| 验证方式 | 静态审计真实接口（`lib/*.synl` 为权威公开 API 镜像）+ 关键实现抽查（`lib/cpp/*.hpp`）+ 实测复现 |
| 语言说明 | 内部评估文档，中文为主、技术术语与标题保留英文 |

---

## 0. 结论摘要与重要更正

> **重要更正（先说事实）**：本评估的前一版严重过时。上一版基于"15 个模块、约 100 个方法、D1–D9 多数为开放缺陷"的描述，但**实际仓库已经进化**：现有 **18 个模块**、约 **300 个公开方法/构造器**；上一版列为"开放"的 **D1–D9 已全部闭合**，`io`/`maths`/`system`/`file`/`async`/`assert`/`hash`/`re` 中曾被点名的"缺口方法"绝大部分已实现。本版是基于真实 `.synl` 接口逐模块重新盘点的结果，而非对旧版的修补。

### 0.1 总体判断

标准库已经从"玩具级覆盖"跨进了**准工业级（L2–L3）的骨架**，但**距离"能放心拿来做生产依赖"还差一层真正的数据交换与工程化能力**，且**测试覆盖与治理严重滞后于库本身的膨胀**。具体：

- **值得肯定的硬进展**（这些是实打实的，不是客套）：
  - `hash`：sha256/sha512/sha1/blake2b/crc32/fnv1a + hmac/pbkdf2/constant_time_equal/uuid_v4/random_bytes/hash_stream，**且标准测试向量逐位正确**（历史审计已确认）。
  - `re`：已弃用 `std::regex`，换为**自研线性时间 Pike VM（Thompson NFA→字节码）+ 有界步数上限**，对灾难性回溯（ReDoS）免疫——这是上一版最担心的 D8，现在已从根上解决。
  - `structs`：Map 键带类型标签（Number 1 与 String "1"、浮点、不同对象实例均不碰撞）、Dijkstra 最短路、AVL 自平衡树、图算法竞赛级 API（LCA/树直径/重链剖分）。D1/D2/D9 闭环。
  - `maths`/`system`/`file`/`io`/`async`：上一版列出的"缺口方法"基本已全部补齐（可复现 RNG、exec 无 shell 形态、路径/目录/原子写、stderr/format/flush、await_all/race/all_settled/state 等）。
  - `assert`：**已升级为真正的测试框架**（equal/is_true/is_false/near/throws + Suite + TAP/TAP-JSON 输出），不再是只有两个反射查询的空壳。
  - `error`/`warning`/`sysapi` 三个新模块已就位（详见 §4）。
  - 一致性：所有库错误即时抛出 + 类 g++ 诊断 + 执行栈；原生状态有 `on_release`/`dispose` 回收；有 GIL 串行锁防并发竞争。零第三方源码依赖，全部原创。

- **真正阻碍"工业级"的判断（按顺序，不是按字母）**：
  1. **错误模型碎片化 + 致命即退出 → `assert::throws` 形同残废**（架构级，且与另一会话冲突风险，见 §3.1）。
  2. **没有数据交换层**（JSON / base64 / 编码）。`http` 取回的是字符串，要解析 JSON 得手写；二进制只能当 String 搬。这是"能不能做实事"的分水岭（§3.2）。
  3. **`http` 无 TLS**。现代 Web 强制 HTTPS，明文 `http` 无法访问绝大多数 API（§3.3）。
  4. **测试覆盖没跟上库膨胀**。库方法从约 100 暴增到约 300，但 `assert_runtimes.cpp` 对新增方法几乎零断言，`verify/philosophy/` 已有 **40 个** demo 却仍**没有任何 golden-file** 接入 ctest（§3.4）。D1–D9 类"静默错误"风险重新出现——方法多了，盯的人没多。
  5. **`re::Match` 偏移是字节而非码点**（国际文本正确性问题，D3 的尾巴，§3.5）。
  6. **缺结构化 `Date`/`Time`/`Duration` 与 `Path`/`Bytes` 类型**（语义欠清晰，§3.6）。
  7. **演进治理空白**：无稳定性分级、无 CHANGELOG、参考文档（`docs/olddocs/Syclun标准库参考.md`）未覆盖新模块且可能仍反映旧 API（§3.7）。

### 0.2 上一版 D1–D9 状态复核（均已闭合）

| # | 旧判缺陷 | 当前状态 | 证据 |
|---|---|---|---|
| D1 | `Map` 键碰撞 | **已闭合** | `structs.synl`：键带类型标签，`keys` 返回原始键对象 |
| D2 | `shortest_path` 忽略权重 | **已闭合** | `structs.synl`：`shortest_path`/`shortest_distance` 走 Dijkstra；新增 `directed`/`has_cycle`/`topological_sort`/`connected_components`/`mst` |
| D3 | `String` 字节索引 | **基本闭合（留尾巴）** | 已加 `char_count`/`char_at`/`substr_chars` 码点 API + 文档声明字节语义；但 `re::Match` 仍为字节偏移（见 §3.5） |
| D4 | `file` 文本模式 CRLF | **已闭合** | `file.synl` 注释声明 `write`/`append` 二进制模式；新增 `write_lines`/`write_atomic` |
| D5 | 原生状态泄漏 | **已闭合** | `on_release` 钩子 + `dispose()` + `async` 有界注册表（1024 条） |
| D6 | `async` 无解释器锁 | **已闭合** | GIL + `launch_detached` 分离线程 + 放锁点审计（见旧版 0.2 节） |
| D7 | `system` 走 shell | **已闭合** | `system.synl`：`exec(argv, timeout, cwd, env)` 无 shell 安全形态；`run` 返回 `(status,stdout,stderr)` |
| D8 | `re` 回溯 ReDoS | **已闭合** | `re.synl`：自研 Pike VM（线性时间）+ 有界步数；已弃 `std::regex` |
| D9 | `Tree` 不平衡 | **已闭合** | `structs.synl`：AVL 自平衡；新增 `height`/`lower_bound`/`upper_bound`/`range` |

> 建议仍对 D8 跑一组已知 ReDoS 死穴模式（`(a+)+$`、`a{30}b{30}c{30}d{30}` 等）做回归确认，因为"自研 Pike VM"的结论来自接口注释与弃用 `std::regex` 的事实，尚未在此轮逐行核验 `re.hpp` 实现细节。

---

## 1. 重新盘点的库清单与成熟度重评

| 模块 | 公开方法/构造器 | 成熟度 | 关键说明 |
|---|:--:|:--:|---|
| `hash` | 13 | **L3** | 算法正确、含 HMAC/PBKDF2/常量时间比较/流式；缺 JSON 配合但自身完整 |
| `maths` | 36 | **L3** | 三角/随机/标量工具齐全；`maths::math` 预置实例 |
| `io` | 21（+3 预置流） | **L3** | OStream/IStream/ErrStream 完整；`io::out`/`io::in`/`io::err` 预置 |
| `file` | 27 | **L3** | 读写/路径/目录/原子写/stat 齐全；与 `io` 流接口仍未统一 |
| `system` | 27 | **L3** | exec/D7 闭合、运行时自省齐全；时间仅字符串/毫秒 |
| `structs` | 74（5 类） | **L3** | 正确性已修；算法竞赛级图/树 API；偏"多"但这次是对的 |
| `re` | 16（2 类） | **L3** | 抗 ReDoS；`Match` 偏移为字节（§3.5） |
| `async` | 21（3 类） | **L2** | GIL 串行；await_all/race/all_settled/state 已齐；无真 I/O 并发、无线程上限 |
| `assert` | 17（3 类） | **L3** | 真测试框架；但 `throws` 对致命错误无效（§3.1） |
| `sugar` | 5 | **L1** | `Infix` 仅 Number；定位尴尬（§3.8） |
| `error` | 1（构造器） | **L1/实验** | 致命 raise；与 async::Error 字段不统一 |
| `warning` | 1（构造器） | **L1/实验** | 非致命告警；返回值空对象，不可查询 |
| `sysapi` | 2 | **L2/实验** | 可扩展 API 派发层；open_url/beep/sleep/clipboard |
| `http` | 11 | **L3（缺 TLS）** | 明文 HTTP/1.1；无 TLS/重定向/重试 |
| `internet` | 6 | **L3** | 接口/连通性探测；不含主动网络管理 |
| `bt` | 5 | **L3** | 发现/连接；RFCOMM 字节流未开放 |
| `gui` | 18 | **L3** | 跨平台 Win32/X11/Cocoa；绝对定位、无布局器 |
| `windows` | 18 | **L3** | 跨平台 2-D 画布；基础图元 |

**加权总评：L2 偏上，向 L3 跨越中。** 与上一版"L1"相比是质变；但**缺数据交换层 + 错误模型断裂 + 测试滞后**三件事，使得"能否在生产里放心依赖"的答案是"还不行"。API 数量（约 300）已远超"够用"，下一步不是继续堆方法，而是**补数据层、修错误模型、把测试与治理追上**。

---

## 2. 评估维度与成熟度模型（沿用）

十二维度（正确性 / API 完备性 / 错误处理 / 并发安全 / 资源生命周期 / 可移植性 / 国际化 / 安全性 / 性能 / 可测性 / 文档 / 演进治理）与 L0–L4 分级沿用上一版定义。本版只更新结论：在"错误处理"（因 §3.1 断裂）、"国际化"（因 §3.5 字节偏移）、"可测性"（因 §3.4 覆盖滞后）、"演进治理"（因 §3.7 空白）四项上，整体仍停在 L2 甚至更低，尽管单模块方法数很高。

---

## 3. 体系性局限（犀利、具体）

### 3.1 错误模型碎片化 + 致命即退出 → `assert::throws` 形同残废（架构级，P0）

这是当前最该被正视的体系性缺陷，且**与另一会话的工作直接冲突，必须优先对齐**。

**事实还原**（从 `assert.hpp` / `error.hpp` 实现与 MEMORY 错误模型条目）：
- 三条错误处理路径并存且彼此不统一：
  1. `rb::native_error(msg)` → 抛出**可捕获**的 `rt_builtin::NativeError`（非致命，调用方拿不到结构化字段，只是字符串）。
  2. `Thrower.throwE(code, msg)` → **直接 `exit(1)`**（致命）。`error::Raise(code, message)` 的构造函数走这条路。
  3. `async::Error`（`kind`/`message` 隐藏胶囊）→ 仅限 `async` 任务内传播。
- `assert::throws(fn)` 的真实机制（`assert.hpp::run_capture`）只 `catch` 了 `NativeError` / `std::exception` / `...`。而致命错误走的是 `exit()`，**根本不抛 C++ 异常**——所以 `assert::throws` **对真正的致命错误（类型违犯、除零、error::Raise）完全无效**：要么进程已退出，要么它压根没机会捕获。它的能力被阉割成"只能验证非致命 native 错误"。

**后果**：
- 语言无 try/catch；致命错误只能终止进程，程序内无法恢复。
- `error::Raise(code, msg)` 与 `async::Error(kind, message)` **两套字段命名不一致**，没有统一的 `Error` 类型层次。一个新的 `error` 模块和已有的 `async::Error` 没有复用关系。
- 测试框架的关键卖点（"断言某段代码抛了期望的错误"）对绝大多数真实错误是废的。

**与另一会话的兼容注意（重要）**：`git status` 显示 **`src/exception_throw.hpp` 正在被另一会话修改**——这正是错误抛出的核心。因此**本评估不对错误类型的具体设计做越俎代庖的规定**，只给出必须对齐的约束：
- stdlib 的错误约定必须与核心重做对齐：统一 `code`/`kind`/`message`/`cause` 字段语义。
- 必须提供"致命 vs 可恢复"的区分；至少让 `assert::throws` 对致命错误能给出有意义结果（判定"抛出了致命错误"，而非进程直接死掉）。
- `error::Raise` 与 `async::Error` 应收敛为共享的形态（或 `error` 提供向 `async::Error` 的转换）。

> 在 `exception_throw.hpp` 重做落地前，**不要**在 stdlib 层自行发明错误类型；任何错误相关的新增都应是"等核心对齐后再接"。

### 3.2 没有数据交换层：JSON / base64 / 编码（L3→L4 的硬缺口，最高 ROI）

当前库能"取数据"（`http` 取回 body 字符串、`file` 读写字符串、`hash` 产摘要），但**没有任何一层把字节/字符串变成结构化数据**：
- 要解析一个 Web API 返回的 JSON，必须手写递归下降或字符串切分——而 `http` 模块恰恰返回的是 `body: String`。
- 二进制（`file.read`、`hash.random_bytes`）只能当 `String` 搬运，没有 base64，跨文本通道（JSON 字段、HTTP 表单、剪贴板）会损坏。
- 没有 URL 编码、没有 hex。

**这是投入产出比最高的"生态补完"项**：一旦有 `json` + `encoding`，`http` 立刻从"取回字符串"升级为"取回结构化数据"，整个标准库从"能演示"跨到"能做实事"。且作者已证明能写正确的解析器（sha256 逐位正确、re 自研 VM），手写 JSON 解析器完全可行，不破坏"零第三方依赖"。

具体设计见 §6.1。

### 3.3 `http` 无 TLS（最大真实世界缺口）

`http` 仅明文 HTTP/1.1。现代 Web 强制 HTTPS，绝大多数公开 API、云服务的端点都是 TLS。一个不能访问 HTTPS 的 HTTP 客户端，在真实场景里**几乎不可用**。

**权衡**：项目坚持"零第三方源码依赖"。TLS 的可行路径是**只用系统已存在的库**（不引入源码）：Windows `Schannel`、macOS `SecureTransport`、Linux 发行版自带的 `OpenSSL`（链接系统 `.so`/`.dylib`，非 vendored 源码）。建议作为 `https` 扩展，保持明文 `http` 的零依赖核心不变（§6.3）。

### 3.4 测试覆盖没跟上库膨胀（最关键的执行力缺口）

- **库方法从约 100 增长到约 300**，但 `verify/unit/assert_runtimes.cpp` 对新增方法（`hmac`/`pbkdf2`/`exec`/`await_all`/`topological_sort`/`write_atomic`/`hash_stream`/`random_int`/`constant_time_equal`/`is_nan`/`argv`/`monotonic`/`race`/`all_settled` 等）**几乎零断言**——逐词检索该文件，上述新增符号合计仅 6 处命中，且多为 builtin 既有内容。
- `verify/philosophy/` 当前有 **40 个** `.syn` demo，但 **0 个 `.expected` golden 文件**，即没有一个被 ctest 自动校验期望输出。
- 后果：D1–D9 类"静默错误"风险重新出现。方法多了，盯的人没多；任何一处回归都会像上一版那样潜伏很久才被发现。

具体整改见 §6.4。

### 3.5 `re::Match` 偏移是字节而非码点（国际文本正确性问题，D3 的尾巴）

`re` 已换自研线性时间引擎（很好），但 `re.synl` 的 `Match` 注释仍声明 `start`/`end` 为**字节偏移**。语言已有码点级 API（`char_at`/`substr_chars`），而 `Match` 仍用字节——于是"用正则找到位置再对字符串切片"在含中文/emoji 的文本上**必然错位**。一个非 ASCII 的 `text[match.start, match.end]` 会切出损坏片段（与 D3 同源）。

修复成本极低：`Match` 同时返回 `char_start`/`char_end`（码点），保留 `start`/`end`（字节）以兼容（§6.2）。

### 3.6 缺结构化 `Date`/`Time`/`Duration` 与 `Path`/`Bytes` 类型（语义欠清晰）

- `system.now`/`time`/`date`/`datetime` 返回字符串或毫秒，无可运算的时间对象（加减、时区、格式化）。任何真实应用（日志、调度、超时）都在手写字符串解析。
- `file` 路径操作（`join`/`basename`/...）返回裸 `String`，没有 `Path` 对象（链式、可比较、可判等）。
- `hash`/`file` 把二进制当 `String` 搬运，没有 `Bytes`（D3 长期方案的残留——"String（码点序列）/ Bytes（字节序列）分离"至今未做）。

具体设计见 §6.2 / §6.5。

### 3.7 演进治理空白（1.0.0 前必须补）

- 无稳定性分级（stable / provisional / experimental）。`error`/`warning`/`sysapi` 是新增的**实验性**模块，却未标注。
- 无 CHANGELOG。
- **参考文档 `docs/olddocs/Syclun标准库参考.md` 已过期**：逐词检索该文档，对 `error`/`warning`/`sysapi`/`hash_stream`/`write_atomic`/`pbkdf2`/`random_int`/`monotonic`/`await_all`/`json`/`Path` **零命中**——它仍停留在 15 模块 / 旧 API 状态，未覆盖任何新模块或新方法。

### 3.8 `sugar::Infix` 定位尴尬（要么做全、要么降级）

`Infix` 只支持 `Number`（无布尔、无字符串、无比较、无函数调用）。作为"表达式求值器"不完整；作为"演示"又占了标准库名分。建议二选一：升级为完整表达式引擎（变量、比较、布尔、字符串拼接、少量内置函数），或在文档明确定位为"演示用途，勿用于生产"。

---

## 4. 逐模块评估（更新版）

> 对上一版已闭合的缺口（D1–D9 相关）只标注"已闭合"，不再复述；重点放在**本轮新发现的缺口**与**模块定位评价**。

### 4.1 `hash`（13，L3）— 几乎完整，只差数据层配合
已验证正确（标准向量）。差距：**无 JSON/编码配合**（见 §3.2），二进制输入靠 `hash_stream(path)` 解决，但产出仍是 hex 字符串，跨文本通道需 base64（缺）。建议：随 §6.1 的 `encoding` 模块获得 base64 后即可闭环。

### 4.2 `maths`（36，L3）— 完整
可复现 RNG（`seed`/`random_int`/`choice`/`shuffle`/`gauss`）、标量工具（`clamp`/`lerp`/`sign`/`is_nan`…）齐全，`maths::math` 预置。无实质缺口。仅需补每方法复杂度/边界文档。

### 4.3 `io`（21，L3）— 完整，但与 `file` 流接口不统一
OStream/IStream/ErrStream + `io::out`/`io::in`/`io::err` 预置，`format`/`flush`/`err`/`write_bytes`/`eof`/`read_all`/`read(n)` 已齐。唯一欠账：**`io::OStream` 与 `file::File` 的写入接口不同构**（`push` vs `write`），无法写出"对文件或 stdout 无差别输出"的代码。建议：给 `File` 也加 `push`/`push_line`/`format` 同构方法（低成本，§6.5）。

### 4.4 `file`（27，L3）— 完整，路径应提升为对象
读写/路径/目录/原子写/`stat` 齐全。欠账：路径操作返回裸 String（§3.6，建议 `Path` 类）；与 `io` 流接口不统一（§4.3）。

### 4.5 `system`（27，L3）— 完整，时间应结构化
`exec`（D7）/运行时自省齐全。欠账：时间仅字符串/毫秒（§3.6，建议 `datetime`）。

### 4.6 `structs`（74，L3）— 正确性已修，偏"多"但这次是对的
D1/D2/D9 闭环，且图/树 API 达到算法竞赛级。唯一注意：方法数最多（74），应确保所有新算法（LCA/树直径/重链剖分/MST）都有断言覆盖（§3.4）。

### 4.7 `re`（16，L3）— 抗 ReDoS，但 Match 偏移为字节
见 §3.5。另：命名捕获组、replace 回调（`replace_fn` 已有）的文档可补；`Match` 加 `char_start`/`char_end`。

### 4.8 `async`（21，L2）— GIL 串行，无真 I/O 并发、无线程上限
`await_all`/`race`/`all_settled`/`state` 已齐（上一版误以为缺失）。欠账：① 无异步 I/O（`file`/`io` 全阻塞）；② `spawn` 仍"一万个任务一万个线程"，无线程池上限（GIL 下它们串行求值，但线程本身不设上限）；③ 无 `timeout_all`/`cancel_all` 编排原语。建议见 §6.6。

### 4.9 `assert`（17，L3）— 真测试框架，但 `throws` 被 §3.1 阉割
`equal`/`is_true`/`is_false`/`near`/`throws` + `Suite` + TAP/TAP-JSON 已就位，`deep_equal` 支持容器递归比较。致命缺陷是 `throws` 对致命错误无效（§3.1）。修好错误模型后，本框架即可成为把 40 个 philosophy demo 转成回归测试的支柱。

### 4.10 `sugar`（5，L1）— 见 §3.8

### 4.11 `error`（1，L1/实验）— 见 §3.1（与 async::Error 字段不统一；构造即致命退出）
建议：等核心错误重做后，统一错误字段；当前在文档标注 experimental。

### 4.12 `warning`（1，L1/实验）— 非致命告警，但返回值空对象，调用方无法查询"警告了什么"
建议：返回携带 `code`/`message` 的轻量 `Warning` 对象，而非空 `Raise` 对象；标注 experimental。

### 4.13 `sysapi`（2，L2/实验）— 可扩展 API 派发层，设计不错
`call(name, params) -> Dict` + `supports(name)` 的注册表派发模式是良好的"跨平台库扩展点"：新库注册一个 handler 即可被调用，未知 API 自动报错（不静默空转）。当前内建 `open_url`/`beep`/`sleep`/`clipboard_*`。建议：将其定位为"未来跨平台能力的统一入口"，并在文档说明扩展协议；标注 experimental。

### 4.14 `http`（11，L3 缺 TLS）— 见 §3.3
建议：系统库 TLS 后端（Schannel/SecureTransport/OpenSSL）+ 重定向跟随 + 超时/重试 + `json_get`（§6.3）。

### 4.15 `internet` / `bt` / `gui` / `windows`（L3）— 跨平台真实实现，定位清晰
基本达到 L3。差距均属"生态与长期演进"维度（如 `http` 的 TLS、`bt` 的 RFCOMM 字节流、`gui` 的布局器、`windows` 的字体/图像），属 L3→L4 已知路径，不在本轮 P0/P1。略。

---

## 5. 横切问题（跨模块）

- **错误模型**：§3.1，架构级，优先于一切。
- **数据交换**：§3.2，最高 ROI。
- **国际化**：§3.5，字节/码点不一致。
- **测试覆盖**：§3.4，执行力缺口。
- **语义类型**：§3.6，Date/Path/Bytes。
- **演进治理**：§3.7。
- **资源生命周期**：D5 已闭合（`on_release` + `dispose` + 有界注册表）；新增模块须沿用同一回收约定（注册表 + `dispose`）。
- **性能**：各库方法复杂度基本有算法保证（Dijkstra O((V+E)logV)、AVL O(log n)、re 线性），但**未在文档逐方法声明**——应补。
- **并发契约**：GIL 串行已确立（无真并行）；但"哪些方法在 async 中调用安全"仍无文档标注。各库 `g_*_mux` 用 `std::recursive_mutex` 保护自身状态表，不等于整调用原子。建议为所有库方法标注线程安全级别。

---

## 6. 工业化方案（具体、可执行、兼容）

> **实施顺序原则**：① 错误模型（§6.0）必须等/配合另一会话的 `exception_throw.hpp` 重做，**不要抢跑**；② 数据层（§6.1–6.2）与类型层（§6.5）是纯新增模块/方法，不碰核心 `src/`，可与另一会话并行，但新增模块需在 `lib/cpp/std_libs.hpp` 聚合，实施时与另一会话协调避免同一文件冲突；③ 测试与治理（§6.4、§6.7）是本轮最该先动的"地基"，且**只动 `verify/` 与文档，不直接动 `assert_runtimes.cpp` 的既有内容**——除非另一会话已合入其改动（该文件正被其修改）。

### 6.0 错误模型对齐（阻塞项，P0，等核心重做）
- 不自行设计；约束见 §3.1。落地后：统一 `error`/`async`/`assert` 错误字段；让 `assert::throws` 对致命错误有意义；`error::Raise` 与 `async::Error` 收敛为共享形态。

### 6.1 新增 `json` 模块（P1，最高 ROI）
手写的递归下降 JSON 解析器（零依赖；作者已有正确解析器能力）。
```synl
// lib/json.synl
$Json {
    @parse[(text[std::String]) ~> (value[std::Object]) { }];      // 失败抛 NativeError
    @stringify[(value[std::Object]) ~> (text[std::String]) { }];
    @pretty[(value[std::Object]) ~> (text[std::String]) { }];
}
```
用法：
```syn
&http; &json;
-(http::Client c);
-(std::Tuple r) << c.get("https://api.example.com/v1/x");
-(std::Dict body) << json::Json.parse(r.get(1).get(3));   // body 字符串 → Dict
io::out << json::Json.pretty(body);
-(std::String out) << json::Json.stringify(body);
```

### 6.2 新增 `encoding` 模块（P1）
```synl
$Encoding {
    @base64_encode[(data[std::String]) ~> (text[std::String]) { }];
    @base64_decode[(text[std::String]) ~> (data[std::String]) { }];
    @hex[(data[std::String]) ~> (text[std::String]) { }];
    @unhex[(text[std::String]) ~> (data[std::String]) { }];
    @url_encode[(text[std::String]) ~> (text[std::String]) { }];
    @url_decode[(text[std::String]) ~> (text[std::String]) { }];
}
```
用途：配合 `hash.random_bytes` / `file.read` 做二进制↔文本安全搬运；HTTP 表单、令牌、剪贴板通道。

### 6.3 `re` 码点偏移 + `http` TLS（P1–P2）
- `re`：`Match` 加 `char_start`/`char_end`（码点），保留 `start`/`end`（字节）。
- `http`：新增 TLS 后端（系统库链接，不 vendored 源码）→ `https`；`Client` 加 `json_get(url) ~> (dict[std::Dict])`（内部用 §6.1）、重定向跟随、超时/重试参数。

### 6.4 测试覆盖追上库膨胀（P0/P1，最关键执行力）
1. **为每个新模块/方法补 `verify/unit/assert_runtimes.cpp` 断言**（优先：hash 全 13、maths 全 36、system 全 27、file 全 27、structs 全 74、async 全 21）。每条断言对应一个"若错则静默/崩溃"的场景（如 `hmac` 已知向量、`exec` 退出码、`topological_sort` 环检测、`write_atomic` 往返闭合）。
2. **golden-file**：40 个 `verify/philosophy/*.syn` 各配 `.expected`，在 CMake/`ctest` 中加一个 `add_test` 跑全部脚本并 diff 输出。
3. **CI 三平台**（Windows/Linux/macOS）跑全量 348+ 断言 + golden diff。

### 6.5 类型层：`datetime` / `path`（P2）
- 新增 `datetime` 模块（或并入 `system`）：`$Date`/`$Time`/`$Duration`，`now()`/`parse()`/`add_days(n)`/`format("YYYY-MM-DD")`/`since(other)`。解决"时间只给字符串不可运算"。
- 新增 `path` 模块（或并入 `file`）：`$Path` 链式（`Path.new("a/b/c.txt").parent().name().ext().absolute().exists()`），统一 file 路径返回类型。
- `io`↔`file` 接口同构：给 `File` 加 `push`/`push_line`/`format`（与 `OStream` 对齐），实现"对文件或 stdout 无差别输出"。

### 6.6 `async` 编排补全（P2）
`Reactor.timeout_all(ms)` / `cancel_all()` / `set_max_workers(n)`（spawn 线程池上限，替代"一万个任务一万个线程"）。明确 async 定位为"结构化并发/任务编排"而非"并行加速"（GIL 语义下）。

### 6.7 演进治理与文档（P1，1.0.0 前必做）
- 稳定性标签：在 `lib/*.synl` 与 `Syclun标准库参考.md` 逐模块标注 stable / provisional / experimental（`error`/`warning`/`sysapi` 标 experimental）。
- 新增 `CHANGELOG.md`（或并入仓库既有变更记录），记录每次新增/破坏性变更。
- **重写 `docs/olddocs/Syclun标准库参考.md`**：覆盖全部 18 模块 + 逐方法契约（复杂度 / 异常 / 线程安全级别 / 边界 / 可运行示例）。上一版参考文档未覆盖任何新模块（§3.7）。

### 6.8 `sugar` 定位（P3）
升级 `Infix` 为完整表达式引擎（布尔/比较/字符串/函数调用），或文档明确定位"演示用途，勿用于生产"。

---

## 7. 工业化 Checklist（1.0.0 前）

**正确性 / 错误模型**
- [ ] 核心错误重做落地（`exception_throw.hpp`，另一会话），stdlib 错误字段统一（§6.0）
- [ ] `assert::throws` 对致命错误有意义
- [ ] `re::Match` 增 `char_start`/`char_end`（§6.3）
- [ ] D8 补 ReDoS 死穴模式回归确认

**数据 / 类型层**
- [ ] `json` 模块（parse/stringify/pretty）
- [ ] `encoding` 模块（base64/hex/url）
- [ ] `datetime` / `path` 类型

**安全 / 真实世界可用**
- [ ] `http` TLS（系统库后端）+ 重定向 + 超时重试 + `json_get`

**测试（最关键）**
- [ ] 新模块/方法全补 `assert_runtimes.cpp` 断言（hash/maths/system/file/structs/async 优先）
- [ ] 40 个 philosophy demo 接入 ctest golden-file
- [ ] CI 三平台全量

**治理 / 文档**
- [ ] 稳定性分级标签（stable/provisional/experimental）
- [ ] CHANGELOG
- [ ] 重写 `Syclun标准库参考.md` 覆盖 18 模块 + 逐方法契约

**并发 / 资源**
- [ ] async 加 `timeout_all`/`cancel_all`/`set_max_workers`
- [ ] 各方法线程安全级别标注

---

## 附：本次盘点方法

- 接口盘点：逐文件读取 `lib/*.synl`（权威公开 API 镜像）统计真实方法数与签名。
- 实现抽查：`lib/cpp/{error,sysapi,assert}.hpp`（确认新模块非桩）、`lib/cpp/assert.hpp::run_capture`（确认 `throws` 机制边界）。
- 覆盖核查：`verify/unit/assert_runtimes.cpp` 逐词检索新增方法符号（合计 6 处命中，多为 builtin 既有）；`verify/philosophy/` 40 个 `.syn`、0 个 `.expected`。
- 文档核查：`docs/olddocs/Syclun标准库参考.md` 对 `error`/`warning`/`sysapi`/`hash_stream`/`write_atomic`/`pbkdf2`/`random_int`/`monotonic`/`await_all`/`json`/`Path` **零命中** → 确认过期。
- 兼容性核查：`git status` 显示 `src/{builtin,exception_throw,interpreter}.hpp`、`src/main.cpp`、`verify/unit/assert_runtimes.cpp` 正被另一会话修改 → 本评估对错误模型只给约束不给设计，且本文件（评估文档）为唯一改动对象，不触碰任何代码/测试文件。
