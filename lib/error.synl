// lib/error.synl
//
// Standard library: error (Synth-OOP interface).
// 标准库：error（Synth-OOP 接口）。
//
// [EXPERIMENTAL / 实验性] The public shape of this library may change without
// notice before the 1.0 freeze. Pin a commit if you depend on it.
// 本库公开形态在 1.0 冻结前可能未经预告即变更。依赖时请锁定提交。
//
// The behavioral face of the `error` library; the real work is done by the
// C++ backend in lib/cpp/error.hpp. Importing `&error;` registers the native
// class, then interprets this file — the class is already registered, so the
// definition here documents the public shape. Any `$Program` class in a
// library is ignored.
// `error` 库的行为形态；真实工作由 lib/cpp/error.hpp 的 C++ 底层完成。
// `&error;` 导入时登记原生类，再解释本文件——该类已登记，故此定义
// 仅作为公开形态的文档。库中的 `$Program` 类会被忽略。
//
// Method signatures below carry their type constraints; the interpreter
// enforces them at the call boundary. The authoritative enforced signature
// lives in lib/cpp/error.hpp's make_sign; this file mirrors it.
// 下列方法签名均带类型约束；解释器在调用边界强制检查。强制检查的权威签名
// 位于 lib/cpp/error.hpp 的 make_sign；本文件是其可读镜像。

$Raise {
    // The constructor raises a fatal interpreter error and terminates the
    // process (g++-style diagnostic + fatal exit). It never returns, so this
    // is in practice a one-line `raise` statement. Writing `error::Raise(...)`
    // as a bare statement is the intended use.
    // 构造函数抛出致命解释器错误并终止进程（类 g++ 诊断 + 致命退出）。
    // 它永不返回，故这实际上是单行 raise 语句。把 `error::Raise(...)`
    // 作为裸语句书写即预期用法。
    @::[(code[std::String], message[std::String]) ~> () { }];
}
